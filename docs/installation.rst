============
Installation
============

At the command line::

    $ easy_install lib-exo-messages

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv lib-exo-messages
    $ pip install lib-exo-messages
